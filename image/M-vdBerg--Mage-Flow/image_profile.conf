# auto-generiert aus config/image_models.yaml — Mage-Flow
PROFILE_LOADER=mage_flow
PROFILE_STEPS=20
PROFILE_GUIDANCE=3.5
PROFILE_SIZE=1024x1024
PROFILE_DTYPE=bfloat16
PROFILE_NOTES='2026-08-11 aktiv (v2). _MageFlowAdapter (loaders.py) übersetzt die non-diffusers-API. FIX: transformers auf <5.6 gepinnt (5.6 entfernte create_causal_mask(input_embeds=) → screen_text-Gate crashte fail-closed → alles blockiert). Jetzt: Screening klassifiziert korrekt (benigne Prompts passieren, Gate intakt), echte Bilder. Text-Encoder sdpa (VF_HF_ATTN_IMPL). Mirror von microsoft/Mage (MIT).'
