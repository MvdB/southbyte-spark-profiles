# vLLM Profil – handvalidiert auf GB10 (2026-08-06)
# Modell  : openai--gpt-oss-safeguard-20b
# HF-ID   : openai/gpt-oss-safeguard-20b
# Manuell anpassen wenn nötig. Wird von vllm_spark.sh geladen.

PROFILE_VLLM_COMPATIBLE=1
PROFILE_DTYPE='auto'
PROFILE_GPU_MEM_UTIL='0.30'
PROFILE_MAX_MODEL_LEN=8192
PROFILE_MAX_NUM_SEQS=16
PROFILE_ENFORCE_EAGER=1
PROFILE_TRUST_REMOTE_CODE=1
PROFILE_KV_CACHE_DTYPE='fp8'
PROFILE_NOTES='Validiert 2026-08-06 auf GB10, vLLM v0.25.1. Start ~120s. Smoke 4/4 (EN/DE). Policy steht als Freitext im System-Prompt, Ausgabeformat gibt der Client vor (hier striktes JSON) — keine feste Taxonomie. reasoning_effort=low ~2.0s/Fall (67 Tok), =high ~3.6s/Fall (160 Tok), beide 4/4 im Smoke. FALLE: ohne TIKTOKEN_ENCODINGS_BASE liefert jeder Request HTTP 500 "error downloading or loading vocab file" — die Harmony-Vorlage zieht sonst o200k live aus dem Netz. Gleiche Loesung wie bei gpt-oss-20b/120b. vLLM trennt reasoning nicht ab (kein reasoning_parser gesetzt), das JSON steht direkt in content.'
PROFILE_DOCKER_IMAGE='vllm/vllm-openai:v0.25.1'
PROFILE_DOCKER_ENV='TIKTOKEN_ENCODINGS_BASE=/hf_models/.tiktoken_encodings'
