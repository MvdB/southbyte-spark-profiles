# Smoke v0.24.0 OK 2026-07-10: 4.0 tok/s (bandwidth-bound, plausibel fuer 30B dense BF16).
# 2026-07-10: Image v0.23.0 -> v0.24.0 (einheitliche Version fuer Vergleichslauf).
# vLLM Profil – ibm-granite/granite-4.1-30b
# Granite 4.1 dense BF16, 30B Parameter, 128K Position-Embeddings.
# Architektur: GraniteForCausalLM (vLLM-nativ, MUP-Skalierungen werden gelesen:
#   embedding_multiplier=12.0, logits_scaling=16.0, attention_multiplier=1/128).
# GQA: 32 Attention-Heads / 8 KV-Heads, head_dim=128, 64 Layer.
# Tokenizer: HF Fast (tokenizer.json), BPE, chat_template.jinja extern.
#
# Speicherbudget Spark (128 GB unified, 85% util):
#   Gewichte BF16 ≈ 60 GB
#   KV-Cache pro Token (FP8): 64 layer × 8 kv × 128 dim × 2 (K+V) ≈ 128 KB
#   max_model_len=32768, max_num_seqs=4 → KV ≈ 16 GB; Total ≈ 76 GB → fits
#   Spielraum für 65536 vorhanden, beim ersten Start aber konservativ.

PROFILE_VLLM_COMPATIBLE=1
PROFILE_DOCKER_IMAGE='vllm/vllm-openai:v0.24.0'
PROFILE_DTYPE='bfloat16'
PROFILE_GPU_MEM_UTIL='0.85'
PROFILE_MAX_MODEL_LEN=32768
PROFILE_MAX_NUM_SEQS=4
PROFILE_ENFORCE_EAGER=0
PROFILE_TRUST_REMOTE_CODE=0
PROFILE_KV_CACHE_DTYPE='fp8'
PROFILE_NOTES='Granite 4.1 30B dense BF16. v0.21.0 (CUDA 13/sm_120 nativ, MRV2-fähig). Native GraniteForCausalLM. max_model_len=32k konservativ (Modell hat 128k Position-Embeddings, KV-Cache wäre bei 4 seqs zu groß). Bei stabilem Lauf auf 65k erhöhen.'
