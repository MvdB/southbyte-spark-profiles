# vLLM Profil – auto-generiert von vllm_spark_profiler.py
# Modell  : microsoft--Phi-4-reasoning-plus
# HF-ID   : microsoft/Phi-4-reasoning-plus
# Manuell anpassen wenn nötig. Wird von vllm_spark.sh geladen.

PROFILE_VLLM_COMPATIBLE=0
PROFILE_NOTES='INKOMPATIBEL mit vLLM. Modell hängt in endlosem Thinking-Loop (hits max_tokens ohne finales Answer). vLLM-Issue #18141 — closed '\''not planned'\'', kein Fix verfügbar.'
